[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm rebuild']

[start]
cmd = 'sh -c "clawrouter setup --yes && clawrouter --port ${PORT:-8080}"'
